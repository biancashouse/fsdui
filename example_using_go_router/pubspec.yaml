name: example_using_go_router
description: "Example website project to demo the fsdui pkg."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 2.9.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  fsdui:  #any
    path: ../../fsdui

  vsc_quill_delta_to_html: ^1.0.5
#  sliver_dashboard: ^1.2.0
#  responsive_bootstrap_ui: ^1.0.5
#  window_manager: ^0.5.1
#  firebase_core: any
#  go_router: any

  webview_flutter_platform_interface: any
  responsive_flex_list: ^1.4.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4

  flutter_lints: any

false_secrets:
  - /lib/bh-apps.firebase_options.dart

flutter:
  uses-material-design: true
  assets:
    - assets/images/
